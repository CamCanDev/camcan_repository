<phone:PhoneApplicationPage 
    x:Class="CamCan.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True" Loaded="PhoneApplicationPage_Loaded">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock x:Name="ApplicationTitle" Text="MY APPLICATION" Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock x:Name="PageTitle" Text="page name" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>-->

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Margin="0,0,0,0" Grid.RowSpan="2" HorizontalAlignment="Right" Width="468">
            <Grid.Background>
                <ImageBrush ImageSource="Images/loginBackground.fw2.png" />
            </Grid.Background>
            <TextBox Height="76" Margin="51,431,46,0" Name="txtUser" Text="{Binding Username}" VerticalAlignment="Top" TextAlignment="Center"/>
            <TextBox Height="76" Margin="51,527,46,0" Name="txtPass" Text="{Binding Password}" VerticalAlignment="Top" TextAlignment="Center"/>
            <Button Content="Login" Height="72" HorizontalAlignment="Left" Margin="154,609,0,0" Name="btnLogin" VerticalAlignment="Top" Width="160" Click="btnLogin_Click" />
            <Image Height="239" HorizontalAlignment="Left" Margin="97,52,0,0" Name="image1" Stretch="Fill" VerticalAlignment="Top" Width="274" Source="/CamCan;component/Images/logo_camcan.png" />
            <TextBlock Height="30" HorizontalAlignment="Left" Margin="65,409,0,0" Name="textBlock1" Text="Username:" VerticalAlignment="Top" Width="194" FontSize="24" />
            <TextBlock Height="31" HorizontalAlignment="Left" Margin="65,505,0,0" Name="textBlock2" Text="Password:" VerticalAlignment="Top" Width="169" FontSize="24" />
        </Grid>
    </Grid>

    <!--Sample code showing usage of ApplicationBar
     <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True" BackgroundColor="#FFC2000D">
            <shell:ApplicationBarIconButton IconUri="/Images/help.png" Text="Help"/>
            <shell:ApplicationBarIconButton IconUri="/Images/login.png" Text="Login"/>
            <shell:ApplicationBarIconButton IconUri="/Images/info.png" Text="Info"/>
                <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>-->

</phone:PhoneApplicationPage>
